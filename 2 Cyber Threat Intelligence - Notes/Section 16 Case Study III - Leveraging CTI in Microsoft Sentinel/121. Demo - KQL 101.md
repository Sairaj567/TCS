---
course: "Cyber Threat Intelligence"
section: "Section 16 Case Study III - Leveraging CTI in Microsoft Sentinel"
lecture: "Demo - KQL 101"
tags: [kql, 101, cybersecurity]
status: draft
---

## Summary
We will now write some [[2 Cyber Threat Intelligence - Notes/Section 16 Case Study III - Leveraging CTI in Microsoft Sentinel/102. Azure Log Analytics|kql]] queries together, especially considering the knowledge that you built up earlier when we talked about the process of creating queries and important operators. Then we make a bit more screen real estate and look for the first table, which will be the security event table.

## Key Concepts
- **KQL** — But I think by now you got the basics of Kql, and you should have a good understanding of how to write and execute calculate queries in your [[1 Cybersecurity Foundation - Notes/5 – Data Security and Encryption|Azure]] environment.
- **[[1 Cybersecurity Foundation - Notes/1 – Introduction to Cybersecurity|IDS]]** — And we see that we only look at the event IDs for four, six, eight, eight.
- **Azure** — But I think by now you got the basics of Kql, and you should have a good understanding of how to write and execute calculate queries in your Azure environment.
- **Log Analytics** — And then what you see is a windows [[2 Cyber Threat Intelligence - Notes/Section 3 AZURE/11. Cloud Computing Properties|virtual machine]] that ingests the logs into log analytics is called Windows Logs.
- **Virtual Machine** — And then what you see is a windows virtual machine that ingests the logs into [[2 Cyber Threat Intelligence - Notes/Section 16 Case Study III - Leveraging CTI in Microsoft Sentinel/100. Sentinel Architecture|log analytics]] is called Windows Logs.

## Detailed Notes

### Lecture Flow

We will now write some kql queries together, especially considering the knowledge that you built up earlier when we talked about the process of creating queries and important operators.
let's head over to logs.
Then we make a bit more screen real estate and look for the first table, which will be the security event table.
We will also remove the pipe operator, and then we can execute the command by either clicking on run or pressing shift enter.

### And then we will see some results at the very bottom

And then we will see some results at the very bottom.
let's leverage the first important operator which is the where.
And for that we simply create a new pipe and say, please leverage the where operator.
And let's look for any event ID that contains the event ID of 4688.

### And then we go for the next filter, which will be please only show me th

And then we go for the next filter, which will be please only show me the last events from the last hour.
In this case we then say time generated.
And we say go with the time span of one hour.
We click on Shift Enter to execute the query again, and then we see that we actually have some results.
We can also look at the results.

### Then we get a bit more bigger for you

Then we get a bit more bigger for you.
And we say we can then see okay for six, eight actually equals to the activity of a new process that was created on our windows VM.
And then we can also, have a look at all the fields that are returned to us as part of this query.
Great.
this is it for the where statement.
Let's continue with the next one which is the limit.

### And take operator

And take operator.
And by the way this is a synonym.
Both of those do exactly the same thing.
What we can do as a very easy statement.
We can expand our query with another pipe operator and say, please only show me five events.

### And again this is fully random

And again this is fully random.
There is no underlying logic that defines which five events are returned.
This is just purely random.
We can also say just take three and then only three will be taken.

### And as you see, this is like purely random

And as you see, this is like purely random.
That's it for taking limit because actually it doesn't get more complex.
We just limit the amount of results that we receive as a return.
let's have a look at the next operator.

### And for this one we could simply say we remove the take and say just cou

And for this one we could simply say we remove the take and say just count.
And if we execute this, you will see that log [[2 Cyber Threat Intelligence - Notes/Section 16 Case Study III - Leveraging CTI in Microsoft Sentinel/102. Azure Log Analytics|analytics]] counts the amount of times in the last hour where a new process has been created.
Yeah.
If we expand this one, you see.
the count is eight.

### All we do is we count the amount for the last hour

All we do is we count the amount for the last hour.
That's it.
We can make it a bit more complex.
However, we can also say instead of just saying count, we could say please summarize, count and do this.
For example, by computer.

### And then we execute this

And then we execute this.
And then what you see is a windows virtual machine that ingests the logs into [[2 Cyber Threat Intelligence - Notes/Section 16 Case Study III - Leveraging CTI in Microsoft Sentinel/102. Azure Log Analytics|log]] analytics is called Windows Logs.
And this one.
Has actually created a new process eight times in the last hour.

### And if we would have more than just one windows VM that does this, you w

And if we would have more than just one windows VM that does this, you would see several rows that are being returned here.
And for every computer, the count of new processes would be visible to you.
In this case, we only have just one VM, which is why we only get one result.
But I think the logic is clear to you.
What we can then also do is we can render a new visualization based on the amount of results we get back.
what we can do is we can remove the summarize count and say please render a new.

### Height chart, for example, and say, please execute this one and then you

Height chart, for example, and say, please execute this one and then you will see that we receive a pie chart back.
What we could do then, to make it a bit more interesting in terms of visualization, is we can go with a different event ID additionally and say wherever the event ID also equals 4673.
And we do this with an Or statement, so that also the results are returned whenever this is also true.
And then we also summarize it.
By we say summarize count.

### Actually

Actually.
Please do this for the event ID.
And then instead of rendering a pie chart, we, for example, can do this with a time chart.
And then it would be rendered and then we get the results back.
But as you can see, this is not that good of a visualization for those data sets because the graph is a bit strange.
let's instead of a time chart, go with a bar chart.

### And then this makes it a lot more easy, because what we see then is we r

And then this makes it a lot more easy, because what we see then is we receive a visualization for the count for the specific event IDs for the last hour, which is pretty great because this is exactly what we wanted to achieve.
From here we continue with the next operator, which is project, and for this one we simply remove the bar chart and execute the query again.
And what you see is that there are certain columns returned, such as the event ID and the count.
But let's also remove the next statement, which is the summarize count and execute the query again.

### And then you will see that all columns of the security event table are a

And then you will see that all columns of the security event table are actually returned.
And to only look at columns that are actually interesting for us.
And this is something we can do with project.
we could, for example, say please only return the columns.
For the event IDs.

### And then you see that the output changes so that only the column for the

And then you see that the output changes so that only the column for the event ID is being displayed to you.
But for us, this is not enough, because we only want to see whenever this is done.
whenever the events are generated.
And if we then add the time generated, you will also see that this is being included in our query.
This is something you can do with project.
Let's go back.

### To our initial statement

To our initial statement.
We also remove to summarize count.
And as you see, all columns of the table security events are now being displayed again in the output.
But instead of like retyping all columns with project, what we could also do is we could say please project away the time generated because we're not interested in this one.

### And then you will see that the output is exactly the same

And then you will see that the output is exactly the same.
It's only that we deleted the column called time generated from the output.
Because it was missing.
It was on the very left side.
We again remove the project away statement and execute the query.
You will see that it is present again.

### That's something you can do with project, manipulating the output in ter

That's something you can do with project, manipulating the output in terms of columns that are presented to you.
For the next operator, we can leverage the distinct operator, and this one will only return unique values.
if we say something like distinct event ID.
And for now, what you see is, that we only receive those two because we said only show those two.
But if we would say something like this one where we remove the filter for the event IDs, you would see every single event ID that has been generated in the last hour, but only once because we only show the distinct values.
That's the entire idea for this.

### Operator and then let's expand our query again a bit more, because we no

Operator and then let's expand our query again a bit more, because we now demonstrate the order.
Operator.
And for this one, we again say wherever the event ID equals.
4688.

### And now we remove the distinct

And now we remove the distinct.
And then we say, please order by time generated and do that in an ascending fashion.
And then we execute the query.
And we see that we only look at the event IDs for four, six, eight, eight.
again, for the new processes that have been created and we order by the time generation in an ascending fashion.
what you see at the very top, we have an event ID for the UTC of five 5456.

### And if we go a bit down we see, okay, this is already changing because w

And if we go a bit down we see, okay, this is already changing because we have five 5457 instead.
this is something we can achieve with the order by operator.
And you can also order by for example, ascending instead.
It also works.

### There are various like values that you can connect to the order by opera

There are various like values that you can connect to the order by operator.
The last operator that I would like to highlight is the let operator, but this one is actually something we will do later on if we work with watch lists and not here, because at this point, we haven't discussed the concept of watch lists or other things that we could reference.
this is something we will discuss later on.
But I think by now you got the basics of Kql, and you should have a good understanding of how to write and execute calculate queries in your Azure environment.

## Terms to Remember
- KQL
- IDS
- Azure
- Log Analytics
- Virtual Machine
- ID
- VM
- UTC

## Possible Exam/Interview Questions
- What does the lecture explain about KQL?
- What does the lecture explain about IDS?
- What does the lecture explain about Azure?
- How would you apply the concepts covered in Demo - [[2 Cyber Threat Intelligence - Notes/Section 16 Case Study III - Leveraging CTI in Microsoft Sentinel/120. KQL 101|KQL 101]]?
- Which examples or tools are used to illustrate Demo - KQL 101?

## Related Notes
See also: [[2 Cyber Threat Intelligence - Notes/Section 3 AZURE/11. Cloud Computing Properties|Cloud Computing Properties]], [[2 Cyber Threat Intelligence - Notes/Section 16 Case Study III - Leveraging CTI in Microsoft Sentinel/100. Sentinel Architecture|Sentinel Architecture]], [[2 Cyber Threat Intelligence - Notes/Section 16 Case Study III - Leveraging CTI in Microsoft Sentinel/102. Azure Log Analytics|Azure Log Analytics]], [[2 Cyber Threat Intelligence - Notes/Section 16 Case Study III - Leveraging CTI in Microsoft Sentinel/120. KQL 101|KQL 101]], [[1 Cybersecurity Foundation - Notes/5 – Data Security and Encryption|Data Security and Encryption]], [[1 Cybersecurity Foundation - Notes/1 – Introduction to Cybersecurity|Introduction to Cybersecurity]].
