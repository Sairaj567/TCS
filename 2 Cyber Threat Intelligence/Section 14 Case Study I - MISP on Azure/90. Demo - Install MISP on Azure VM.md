All right.

After we have set up the virtual machine, we can of course install Misp on it.

And to do that we need to do one step.

Additionally before that.

And that is we will need to modify the network security group of the virtual machine so that we can

connect to it remotely in order to install Misp.

And to do that, just head over to the virtual machine that we just created.

And then um, we can click on the network settings.

And from here you see the default settings for the network Security group.

Currently we will not be able to connect to this network security group.

So what we need to do is we need to add an inbound port rule for SSH.

And we can do that by just clicking on create port rule and then selecting Inbound port rule.

And then for the source you can specify your IP address and what you what you will see then is that

the Azure portal will automatically fill your IP address in this field.

So currently source is set to any.

But if you set it to my IP address, you see that the source IP will actually reflect your IP address.

And then for the service instead of custom we go with SSH.

And then you see that the destination port is actually also changing for the default port of SSH which

is 22.

And from here we can simply add the rule to the network security group.

Um, and you see that it is already being reflected in the portal.

Um, just a short disclaimer.

It may take a few minutes for this change to be active.

So if you follow the next steps and get a connection error, chances are pretty high that you were simply

too fast and that the changes to the network security group have not been processed in Azure for the

next steps.

What you can do is you can simply select connectivity on the left side, and from here we can select

the option to connect remotely via SSH by leveraging the Azure CLI.

And therefore we just click on select.

And you can see that um, there are certain things that the Azure portal is validating for us.

And actually you could leverage a very nice feature called Just in time access to connect to the machine

remotely.

But this would require you to have another paid service active, which would be the defender for service

plan.

In our case, we don't have that, and therefore we switch to the option that says connect from your

local machine.

And then you can see that you can, on this button, decide, um, from which local operating system

you are connecting, because then the options will be a bit different.

So you can say, I am connecting to this Linux machine from a windows machine or from Mac OS, or again

from Linux.

In my case, it's Mac OS.

Um, and the options are a bit different here.

So what you need to do is you of course need to have those prerequisites fulfilled.

Um, we did that by modifying the network security group already.

We can ignore that one because we don't need just in time access.

Um, if you have Mac OS, you also need to install.

You also can go with the option of installing homebrew.

Actually, for the Azure CLI you need that for windows this is not required.

And then we can simply open a shell and go with AZ login uh to authenticate to Azure.

And from there move to the virtual machine.

So just open a terminal.

And then in the terminal you can actually.

Do something like this.

AZ login.

This is also exactly the same step for windows and for Linux.

And then you see that a web browser is actually opening and that looks like this.

And from here you just pick your account again, like you would for example in single sign on and then

you get the message, congratulations, you have actually logged into Azure so we can head back to our

CLI.

And from here you see great.

We are actually connected successfully to Azure.

So what we can do is if we head back to the portal, you see that we are presented with the option of

simply copy and pasting this command over into our terminal again.

And this is again exactly what we will do.

We just click copy and head over to our terminal.

Again.

Copy it, but we will not immediately execute the command.

And the reason for that is when we create the VM.

We also specified a user with a password and that is something we also need to reflect in the SSH login

session.

In my case, the user was called Misp.

So what I will do is I type Misp with the ad symbol here plus the IP address.

And this simply tells the Azure CLI that I want to connect with the Misp user.

So a new.

In your case, if you went with a different username, you need to specify this username over here.

And then we can press enter.

And we see that the connection is opening and we are prompted for a password.

So this is the place where you specify the password that you set up during the creation of the virtual

machine.

And after you provide this successfully, you see that we actually have a remote shell open to the Azure

Virtual Machine.

So from here we can actually install Misp directly on the virtual machine.

And to do that we had over again to a browser window.

And we go to a website that is called Misp dot Github.io.

And you will also find the link for this in the resources section of the video.

And from here we need to follow a few steps.

We could either go with this one, because this option would allow us to make the best choices for installation.

We don't need that, we just go with Misp core and the correct command to do that is simply just to

copy this one, head over again to our shell.

And then in the shell, simply paste the command.

And execute it.

And then you will see that the installation starts to run.

Um.

I can already tell you that it will take at least a few minutes.

In my case, most cases it took like 10 or 15 minutes, but there is some interaction required in between.

You will see that during the next minutes because sometimes we are prompted for restarts, sometimes

we are prompted for options.

So unfortunately you cannot leave the desk and return after 15 minutes.

There is some interaction required.

So we will simply let it run.

And whenever there's interaction required, I will guide you on with which action to go.

For example, in this case, we are prompted with the question of which services should be restarted.

And for all of those questions, there will be multiple.

We can simply click enter and let the installation continue.

We don't need to make any changes, just go with the default option whenever you're prompted for a restart.

And again another prompt for restart.

And again we can simply select enter.

And another prompt with the restart and we again select enter.

And now we have another prompt.

In this case we are asked whether.

The script should create a user called Misp.

Or if you would like to continue as Misp, I simply say yes.

And again we are prompted for restart.

Just click enter again.

And another restart.

Just hit enter.

And another restart.

Just hit enter again.

And for this restart again.

Enter.

And another restart we just specify enter again.

In another restart, we just hit enter.

And then after the installation has completed, you will see that we are presented with some credentials

and you should take note of those.

And no worries.

In my case, the credentials are currently displayed in clear text, but the instance won't be alive

after I publish this course, so it's not an issue from a security perspective at all.

Just keep, um, a note of your credentials especially.

Um, the root db passwords and the local system credentials and your misp default credentials.

However, those will be the same across every single Misp instance because those are the default credentials.

As soon as you log on to the Misp instance for the first time, you will be prompted to change those.

Okay, so let's head over to our Misp instance.

And what we can do is we can simply copy this public IP address, paste it into a new browser window,

and then specify Dash users dash login.

And this will actually not work.

And the reason for that is because we have not we haven't specified an inbound rule for Https in our

network security group earlier.

The reason we didn't do that is because I simply wanted to demo you that we of course need to do this

for every single port, not only for Https and SSH, but for everything that requires connectivity to

the VM.

So what we do is we head back over to our virtual machine and create an inbound port rule for https

and HTTP.

In my case, you can see that I did that already.

Um, but the steps for that are, as you saw with SSH, rather straightforward.

Just create and click Create rule.

Select inbound rule and then select your IP address.

And then for HTTP just select HTTP and then just repeat the step for Https in a second rule okay.

If you did that and you waited a few minutes so that the network security group rules can be applied,

you should see the following.

If you access Misp under a new browser window, which is done by simply copying the public IP address

from, for example, over here to a new browser window and then typing, um, the IP address slash users

slash login.

Um, depending on which browser you use, you will get a notification like this that simply says, hey,

um, the certificate is actually invalid.

Um, but in Safari, I can simply say visit this website and depending again on the browser, like in

Chrome, Firefox, you get different notifications, but just skip those and continue to the website.

And then you are facing this login page of Misp.

What we need here is actually the default credentials that we saw earlier.

Which is admin at admin dot test with a password of admin.

No, I don't want to save this.

And then we are actually prompted, uh, immediately after successfully authenticating to Misp to change

our password.

And I will actually do that and go with the option to choose my own password.

Then we can submit it and we are successfully authenticated.

I highly encourage you to change your password as well because as we saw, Misp is actually residing

in internet.

Yes, the network security group configures it in a way that only you can access it with your IP address.

Nevertheless, having default credentials in a potentially publicly facing web application, if you

remove the nsgs at least as something that you obviously don't want at all.

So just change it and then we can continue with the case study during the next lesson.