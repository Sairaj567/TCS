All right.

We just had a look at all tactics, techniques and sub techniques that Apt41 is leveraging in the Attack

Navigator.

Now let's have a look at one specific technique and sub technique.

And for this one we will go with the.

Technique T1003 and the corresponding sub technique, which is always credential dumping.

Let's hop over to the technique.

Um, this one is called OS credential dumping.

It's all around the fact that adversaries may attempt to dump credentials from your from your operating

systems.

And this is, of course, incredibly common, um, across multiple campaigns and of course, with basically

every group, um, most of the adversaries leverage at least some form of this technique.

And in fact, what we will do is we will focus on the else's memory.

So for that one, we just expand the sub technique and click on Alsops memory.

And then we get a description on what this specific technique is doing.

And now, of course, because it's a sub technique we talk a lot about.

Um, yeah, more in depth technology than in comparison to tactics and techniques.

And what we see here is for this specific sub technique that adversaries may attempt to access the credential

material that is stored in the process memory of the local security authority subsystem in windows that

is actually windows specific.

That's not existing in Linux.

Um, therefore we talk about windows only.

And the idea is, um, how this actually works is after a user logs in, the system, generates and

stores a variety of credential material in your lsass process memory.

And these credentials can be harvested by an administrative user or system and used to conduct lateral

movement.

So the thing is, if you log into a windows system, um, those credentials are stored and there are

certain tools such as Mimikatz, which we will have a look at later on, um, that can exfiltrate your

credentials if your host has been compromised.

And below you actually find, um, a lot of syntax with regards to mimikatz.

And of course, as always, miter is also telling you, um, how this sub technique has been leveraged

as part of procedures.

And in this case we also see, um, our beautiful apt41 that is referenced here.

They actually used Hexdump, Mimikatz and the windows credential editor to dump password hashes from

memory and authenticate users to accounts.

And because it's super prominent, we will have a look at Mimikatz.

Um, we will not cover all tools, but Mimikatz is super prominent.

Um, so that's why we have a look at it in more detail.

Scrolling down.

We also see mitigations and detections, but we will cover them later on for Mimikatz.

Let's have a look at Mimikatz first to see how this tool is actually working and what we can do against

it.

Mimikatz is a tool that you can actually get from GitHub.

You can just download it, install it and work with it.

I would, however, highly encourage you to not do it on your work laptop because otherwise, um, your

SoC may have to contact you immediately because they will simply assume that you are an attacker that

wants to exfiltrate credentials.

Mimikatz has caused a lot of headaches to a lot of security teams in the world, because it's super

effective and super easy to execute.

All you need to really do is you need to have access to a to the host system that you want to compromise.

And if you have that and you can download and install software, then you can leverage Mimikatz.

Um, after you download and install it, you can, for example, run certain commands.

You could, for example, do things like this.

Um, you, um, just privilege your debug your privileges, and then Mimikatz will tell you, okay,

um, you've got the privileges to continue, which is great.

And from there you can simply type one line of code, which would be SDK url s a um, colon colon log

on passwords that would um, you being interacting with the LSA in windows.

And what will happen from there is Mimikatz would actually, um, exfiltrate the NTLM hash.

This one would allow you to authenticate, um, against Active Directory, for example, with a so-called

pass the hash attack.

But what you also receive, and this is a bit scary I think, but it's yeah, how Mimikatz works.

You receive the username, the domain and the cleartext password of the user that has logged in.

And this is really all you need.

Again, you need access to the host.

You need to be able to download and install software.

And from there, um, just debug your privileges.

And if they are fine, execute this command and then you receive NTLM hashes together with user username,

domain and password.

How does this work?

Technically?

Um, since we talk about the Midas sub technique of dumping OS credentials from Alsace, we need to

also talk about that.

And not only about Mimikatz, because, again, Midas is focused on behavior instead of tools.

Mimikatz is just a tool that executes that.

It's a very nice tool, but we talk about behavior and not about individual tools.

An attacker, as I mentioned, needs to have system access in order to successfully conduct this technique

and sub technique.

And if he has what he can do, he is he can, for example, extract the credentials with Mimikatz from

Alsace and then um, if, for example, the privileged user has at some point in time locked in to this

device, um, the attacker not only has access to credentials from this user, he may also have access

to the privileged identity, which is super common in enterprise environments.

Um, most big companies, um, and the companies that are security aware, they want to separate identities.

So they don't want privileged identities to log in to non-privileged devices.

Still, that happens a lot.

Meaning if you compromise this device, chances are not that low, honestly, that you also receive

privileged credentials if you dump, um, those from Alsace via Mimikatz.

And if you do, and you are maybe a bit lucky, you can also laterally move from this device over to

a privileged device.

And because you got a privileged identity basically, and you can do the same over there and extract

the credentials with mimikatz and from there, just laterally move through the organization.

That is how this specific middle technique and sub technique is working in combination with Mimikatz.

Let's have a look at how we can defend defend against this.