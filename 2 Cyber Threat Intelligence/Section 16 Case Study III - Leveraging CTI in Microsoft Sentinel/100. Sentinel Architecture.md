Let's talk about the Sentinel architecture a bit more in detail.

And let's also start on the left side for this one.

In the public cloud itself, when we talk about ingesting logs into sentinel, we can do that from basically

anywhere.

It doesn't matter which hyperscaler we talk about can be Azure, can be AWS, GCP or Alibaba or others.

We can take those logs and ingest them into log analytics.

You can do the same and should do the same from on premises.

And it doesn't matter if we talk about Windows or Linux host.

Doesn't matter if we talk about other third party security providers such as Palo Alto or Cisco.

We simply take those logs and ingest them into log analytics via a central feature called connectors.

No worries, we will look at all of this in detail.

This is just intended to give you an overview before we start with these features first.

Of course, we also ingest threat intelligence that can be external and Microsoft threat Intelligence

actually already included.

Just to make it, um, pretty clear, that is something you don't need to set up.

This is done in the back end by Microsoft already.

When the logs arrive, and log Analytics, which is the log backbone of Sentinel.

Basically, we then can start working with Sentinel on those logs by using the Sentinel features that

we will talk about in detail during this course, which is incidence which is the analytics rules itself.

So the use cases that you develop in your SIM, which is notebooks, that is integrating Jupyter notebooks

into Sentinel workbooks which provide visualization and dashboarding hunting, which of course the purpose

is to do active threat hunting and then also playbooks, which is the server functionality.

And all of this is something that you can manage via infrastructure as code from repositories like Azure

DevOps and GitHub, which is pretty cool.

Other source code repositories are currently not supported and that would be great to have.

Currently we only talk about Azure DevOps and GitHub.

![[Pasted image 20260730224713.png]]