Now, you may have wondered what are typical data sources for a SIM like Sentinel?

And this of course varies heavily between the various security operations centers.

But generally speaking, a lot of data sources are quite typical in order to be ingested.

Let's start with the top layer, which is the application layer.

And here we really talk about the apps ingesting their various application specific logs into Sentinel.

We talk about stuff like SAP.

Common use cases for this is for example SAP access logs or various transactions.

We also talk about stuff like ServiceNow as an ITSM tool or workday as your HR application.

But keep in mind when we talk about ingesting application layer logs, we not talk about ingesting logs

from the underlying servers because that would be the operating system.

Instead, we talk about logs directly from the application layer logic as I mentioned, such as access

logs for an SAP system or transactions.

Next, typical data sources would be anything from the network layer, such as the Azure Firewall,

your network security groups in Azure, or your web application firewalls in Azure.

But of course, this can also be from other cloud environments such as AWS or GCP, or maybe also from

your on premises network.

For example, you maybe have on premises firewalls, you may have a proxy solution, stuff like that.

This is quite, quite common to be ingested into a SIM solution.

The next quite typical data source for a SIM is of course anything with regards to the operating system,

such as your Windows Security event logs, anything with Linux, for example syslog common event format,

but also for Mac OS because of course you want to have the visibility for your clients and your servers

also in your SIM solution.

Then especially with um enterprises moving to cloud more and more, of course, the control plane or

platform logs are quite important to be ingested.

This can, for example, be stuff like entry ID, um, something like the sign in logs or the audit

logs from entra, but also Azure Activity, which is basically an audit trail for the control plane

in Azure, or something like logs from an S3 bucket in AWS.

This is all what we commonly refer to as platform or control plane logs, depending on the service.

And this is something that is also quite worth ingesting into a SIM.

And then as the very last, um, big topic, we also of course have our own security solutions.

Solutions.

Yeah, stuff like defender for cloud or defender for Endpoint or Microsoft purview is quite common to

be ingested into Sentinel.

But this of course, is also the fact for other third party solutions that provide, for example, an

EDR service or something like that.

Again, this is not something that you can simply take and apply to any SoC in the world, but the various

topics or the various domains such such as ingesting logs from applications or the network or the operating

system.

This is something that you will see with basically any SoC in the world.

