All right.

So let's verify whether we successfully configured our inter ID connector.

And we can actually do this via two ways.

One of them is viewing under the data connectors blade for the inter ID connector.

And there we see okay the audit logs are actually actually successfully ingested.

We have six events with regards to the audit logs.

Just ignore the sign in logs.

Yeah.

But for the audit logs we have six successful events which is great.

Let's also verify this and log analytics directly.

For this we click on logs.

And then under Log management we click the drop down button and select with a double click the audit

logs.

And then we click on run.

And by doing this we actually see the events that are part of the intra audit log.

And we saw that we just recently added a new user which is exactly what we did.

And if we select this specific event by clicking on the button, you actually see all details with regards

to the user we created in a section called Target Resources.

And there you see the user principal name which equals the one that we created.

In my case the user was called trigger audit log.

In your case the name may of course be different.

So this is actually pretty nice because we successfully set up the ingestion of entry ID.

And now we can later on start working on analytic rules.