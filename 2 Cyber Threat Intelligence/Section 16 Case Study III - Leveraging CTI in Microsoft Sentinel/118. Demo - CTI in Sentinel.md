So let's have a look at where and how we can actually utilize the threat intelligence that we ingested

earlier in Sentinel.

And for that we just click on the Threat Intelligence Blade.

And there you will see a lot of info with regards to threat Intel.

Let's start at the very top.

We see that we currently don't have any alert associated with our threat intelligence, but we see that

currently in Sentinel we have 2800 threat intelligence indicators.

And an indicator is everything that you see here as an individual entry.

So for example, this first one that is actually an indicator of compromise.

And this one relates to a specific URL which is actually this one.

And this one.

This URL is associated with malicious activity.

Yeah.

This is this is when we talk about an indicator we mean really every single entry here.

And we also see the amount of threat intelligence sources that currently ingest their feeds into Sentinel.

And this is for the moment just pulse dive because that's the one we set up for ingestion earlier.

What you can do is also you can filter for the type of indicator that you want to see.

Let's say we want to only view domain names.

That's something we can easily do.

And then you see under the name, we still have the detection pattern because we talk about IOCs.

But then in the value you see the actual domain that is associated with malicious behavior.

You see the source which will be pulse dive because we only have one t source.

And then um, we can also select different filters.

For example, we could filter by source.

In this case we only have pulse dive.

We could filter by thread type.

Let's say please show me everything that is considered malicious activity.

And then we see all IOCs that are strictly associated with malicious activity, for example, for other

data sources, for example, for defender threat intelligence, you could actually also filter by confidence

level.

That is something that is not supported by pulse dive.

Impulse drive.

As you see on the right side, we don't have a confidence level provided.

That is something that varies heavily from threat free to threat feed.

In this case, pulse drive does not do it.

And then we can also filter for the last one which is the expiration.

But that's it.

You could, in theory also add new IOCs completely manual.

So you could say, I want to add a new indicator, which is what we clicked on and say, I want to add

a new domain name and then you could provide your domain.

Same with files.

You could upload hashes in here.

Um, same with IPv4 and IPv6 addresses or with URLs.

All of this is possible to being added manually, but of course that's not something you would do at

scale.

You would simply ingest threat feeds.

What you could also do is you could import threat Intel based on files.

So for example, if you have a CSV file you could import this indicator and then it would arrive in

your threat intelligence overview.

Of course this is also not something you do at scale.

But if you have a big, big file from somewhere that you want to ingest completely possible, and you

actually don't need to set up a new data connector to do that.

And then at the very last, what we can also do is we can import threat intelligence using Te solutions.

And if you click on that, what you will see is we will get forwarded to the threat intelligence solution

from the content hub that we already created earlier.

So let's go back to the threat intelligence and you will see that we basically discussed every feature.

And now during the various sections we will explore how we can leverage that intelligence as part of.