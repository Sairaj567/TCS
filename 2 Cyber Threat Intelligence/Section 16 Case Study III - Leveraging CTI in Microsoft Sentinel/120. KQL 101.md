All right.

So let's discuss the Kusto query language in detail.

When we look at how to construct a Kql query and how the development process should look like.

In most cases we always start with a hypothesis, meaning we always ask ourselves, is there anything

we want to either prove or disprove?

And when we talk about security, this can, for example, be stuff like is this IOC part of my logs?

Right.

Because if an IOC is part of your logs, you may have a big issue.

That is what we call an hypothesis.

And by executing queries, we can either disprove or prove that the hypothesis hypothesis is actually

true or not.

And then in the second step, we of course have to determine the required tables because as you maybe

saw earlier, when we ingest data from on premises from other hyperscalers such as AWS, from wherever

data source basically into log analytics, what happens is log analytics creates a new table for that.

And when we want to validate or invalidate a hypothesis, we of course need to verify where we can actually

get this info from.

And this is where we had to have to determine our tables.

Right.

So if we want to look for IOCs in the windows security event logs, we have to look at the security

events table.

That is the entire process.

And then if we look at the third step, which is exploring the table schema, we of course have to consider

the schema of the table itself.

So this means what fields and attributes are actually present in the tables.

So if we again look at the security event table what we really want to see is do we even have the capability

to look for this IOC in the specific table based on the schema and the fields and attributes that is

offered offering to us?

And then in the fourth step, we of course filter unnecessary stuff away.

This means if we look at the table, we of course are only interested in the stuff that helps us to

disprove or approve our hypothesis, and in this case, the rest is not necessary.

So we just filter it away.

And the fifth step is a bit optional, but Kql also offers you the ability to visualize stuff.

For example, you can render pie charts or time charts to make it more compelling to analyze the data.

If we look at this in a bit more detail.

So the process on how queries are actually constructed after we created our hypothesis, we always start

with the table.

And in this case we talk about the security event table.

And in the next step I mentioned it already.

We try to filter stuff away.

And in this case you see a very simple statement.

And no worries we will have a look at this syntax in way, way more detail than here.

But we leverage the pipe operator to separate the table from the first command.

And then with a statement called where we look for wherever an event ID equals the event ID of 4264,

which equals the event for a successful login.

So this is what we do.

In the next step we take the table where all security events are contained.

And then we filter away based on one specific event ID.

And this of course reduces the amount of returned security events dramatically.

And in the next step.

We summarize the count of successful logons by account.

So what we do is we aggregate the data that we got from the filtering earlier a bit more and just say,

please count all successful logon events that belong to a specific account.

This is our condition in this case.

And in the very last step we say only show me the top ten accounts based on the count that we have identified

earlier.

And this is then present in the very end as an evidence.

Looking at how ingestion actually works.

I mentioned it a bit earlier already.

All data sources that you connect into log analytics are ingested in a table.

So if we look into the windows event logs for example, if we ingest those.

Into log analytics.

What happens is log analytics creates a table called event.

And then we can query this table with Kql.

Same with Linux.

For example, if we ingest syslog into log analytics a table called syslog is created.

And then we can also query this table with kql.

Of course.

Looking again at a statement.

There are also some questions that you can leverage when you construct your own kql statements.

And this is something like the following.

Again, we look at the table called security events.

And then we can ask the first question is what do we actually want to see.

We want to look for security events.

Right.

And this is why we take this table.

Second question that we can ask ourselves is where can we actually prove or disprove our hypothesis

with?

And this is the event ID in this case, right.

Because we just filter for this one and we want to have a look at the successful logon events.

The next step would be aggregation.

So in this case we leverage the summarize count aggregation function, which again counts and summarizes

all successful logon events that we filtered for earlier and does this by account.

And then the last topic would be the ordering.

So in this case we just say please order by the account and do that in an ascending fashion.

That's a very typical process that you can also leverage for your queries.

You always ask with the question, what do we want to see?

Where can I get this data from?

And then we aggregate in order stuff.

And then in the very end we could optionally also visualize it.

But this is the process that you will leverage for like 90% of your queries, if not more.

And then there are also some important operators that you can and should have a look at, because they

are like the most leveraged operators in the majority of your queries.

You saw it already the where operator, super crucial to filter away stuff and reduce the amount of

returned results in your queries.

That always works on a specific predicate as you saw.

But then you also have operators like search, which for example, looks for all columns in the table

for a given value.

So you could do something like search and then insert a string.

And then Kql would loop through all your logs and try to identify which columns in your table actually

contain this value.

And then we also have operators like Limit and Take which return a specific number of records.

So you could do something like please limit the output to ten values or ten number of records actually,

and then only ten would be returned and keep in mind that this is random.

There is no underlying algorithm that only returns a records based on on a given logic.

Instead, it's purely random.

And then the next operator that is quite often leveraged.

And we did that also in our queries already is the count operator.

This one simply counts the records from the input table.

Then we have summarized that something we also did.

This one groups the rows according to the um group columns and calculate aggregations over each group.

We leverage this also already and then we have render.

This one just creates a graphical output such as a pie chart or a time chart.

You can get somewhat fancy with it.

However, keep in mind that this is limited to this render function in Kql and you don't have access

to like fancy math libraries in Python or something.

If you want to get more fancy with your visualizations, Kql is the wrong way to go.

Instead, leverage notebooks as part of Sentinel.

Then we have the operator called extend.

This one creates a calculated column and adds it to the result set that we get returned in the very

end.

Then we have project.

This one selects a column to include in the very order specified.

And there are also like um operators that you can connect with projects such as Project Away.

But we will have a look at those.

Then you have the distinct operator.

This one produces a table with the distinct combination.

We will also have a look at this one.

And then we have stuff like sort that we also leverage there will be sorted by ascending.

You can also sort by descending or by other metrics.

Um just to make your output a bit more compelling.

And then we have the let operator.

This one is also super crucial because what it does is it creates a temporary variable that you can

leverage in your query and reference as part of your query, meaning you can, for example, import

other Sentinel features like a watch list that only exists as a static value, and you can assign this

watch list to a temporary variable with this let statement and then reference values in your watch list

such as VIP users, um, IP addresses, and so on as part of your query, which is super nice.

And then we have the last two statements which are union and join.

If you're familiar with SQL, those may be familiar to you because what union does is it takes two or

more tables and returns all their rows.

So we actually union, um, as the statement says, the tables and join does the same thing, but it

merges only the rows of two tables based on certain conditions.

And again, no worries, we will have a look at those operators, um, in the demo.


