We will now go ahead and create our first scheduled analytic rule.

And for that we go on analytics.

Select the scheduled query rule type.

And then we start with the definition of the rule itself.

What we will do in this one is a bit earlier we ingested entry ID and we verified that the onboarding

was successful by querying audit logs and actually looking for new users that have been added.

This is exactly what we will do here in this rule as well.

Just to teach you the logic of how to create a scheduled rule, it's a very easy example, and maybe

also one that is not quite typical for SME, but it's very nice to provide you with the logic and the

tools to build more complex queries.

So let's start with this one.

The rule name for this one will be new user added.

To enter, we can leave out the description.

The severity will in this case stay medium, but you can also go with the different one if you prefer.

The middle tactics and techniques is something we won't cover and to be honest, it also doesn't make

sense for this one, but we will discuss middle later on in a specific section.

The status is enabled for this rule because we want to work with it immediately, and now it's about

setting the rule logic.

And I copied the query over.

Just to give you a bit of detail, because you may be not familiar with KCL at this point.

What we do in this query is very easy.

We specify in the first line that we want to query the table called audit logs.

And it's also earlier.

This is the table that is originating from our data connector for intra.

And then we filter the table based on this statement where we say please show me all the logs where

the operation name equals add user, which is exactly what what you want to look for.

We could also already look at the query results.

And then as you see.

Uh, Log Analytics will open and give you back the results.

But now we will continue to edit the alert.

And what we can do now is we can additionally enhance the alert by providing entity mappings, custom

details or details on the alert.

This is something we don't need to do for such an easy statement.

What we need to provide for every scheduled query.

And this is why they are called.

Scheduled is the schedule of the query that is running on Sentinel.

At minimum, we need to go with five minutes.

If you go below five minutes, Sentinel will tell you, hey, the value needs to be between five minutes

and 14 days.

In our case, we can actually also go with five minutes, and you also need to specify the lookup data

for the query.

In this case, we actually also go with five minutes.

Sometimes you have an ingestion delay for your data sources where this setup will run into issues.

This is something we will discuss at the very end of the course when we talk about advanced concepts,

because this is something we don't need to know.

Now, just keep in mind ingestion delay is something you need to address accordingly.

In Sentinel, we also want to, uh, start running our query automatically.

Um, basically from now on.

And what you can do next is you can define an alert threshold, meaning there is only an alert generated

if it passes this threshold.

In this case, we defined it to zero means as soon as we add our first user, then we will have our

first alert generated.

If you put this to 0 to 1, we would actually need to create two users for this alert to fire.

But we keep it at zero.

And then we also have the option to group events.

In this case you can either say please group all events into a single alert or trigger an alert for

each event.

In this case, we just leave it with the grouping.

And at the very end, you can also decide to stop running the query after an alert is generated.

This is, of course, um, not very good in this case.

So we leave that and turn leave it to off.

And then we click on next for the incident settings.

And we see that we also have the option to say Dear Sentinel please create incidents from this alert.

And this is something we of course want to have.

And we also just um, leave the alert grouping to disabled because we don't want to have it for this

incident, but this is actually something you can do in most cases.

It it's something that you won't configure.

We can also specify an automated response.

This is something we want to now because we have a dedicated section for this which is dealing with

automation rules and how to deal how to set up Somwa in Sentinel.

And then after that we click on Review and Create and we see all details and we again see the query

that we provided.

And we click on save.

And we see that now we actually have one active rule.

We see the rule severity.

And we see, okay, we actually have one rule that is um, that has a medium severity associated with

it.

We see the name of the rule.

We just provided, the rule type which is scheduled to status, which is enabled.

And we also see that this is actually a custom rule.

So as you as you just saw, we develop this rule on our own.

And then if we select the rule in the flyout you again see details on the query itself.