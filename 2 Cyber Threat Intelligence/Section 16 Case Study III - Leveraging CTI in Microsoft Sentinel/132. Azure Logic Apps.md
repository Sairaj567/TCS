Let's have a look at two really nice examples for Azure Logic apps that you can use as part of your

playbooks.

And the first one is actually a Sentinel playbook that triggers this logic app, which then automatically

creates a ticket in ServiceNow that contains the content of the respective incident in Sentinel.

That is a use case I see quite often because a lot of customers use ServiceNow as their ITSM tool,

and what they do is they take the incident, the incidents from Sentinel and forward them to ServiceNow

so that everyone can closely collaborate on the incident at ServiceNow.

And actually for ServiceNow, logic apps already have a pre-built connector, meaning you don't need

to write a single line of code.

All you need to do is to select the correct actions, and then this one will work.

If we look at the second example.

Also something I see quite often, and that is a super nice example of letting your end users participate

in your sock operations, because this one allows users to actually complete a Microsoft form, and

this form then uploads the content that the users put into the form and create an incident in Sentinel

directly.

Which is pretty cool, because think of scenarios where users that are like not pretty experienced in

terms of cybersecurity or even IT because they work in a completely different part of the business.

Um, assume that they have an active breach on, for example, their device.

This can be scenarios like a user complaining that his mouse is moving and he doesn't know why.

Or it can be users that say, um, I cannot access my drive anymore.

Maybe it has been encrypted and I'm a victim of ransomware, stuff like that.

Simply, this solution allows your users to interact with your SoC directly without relying on common

things like a group mailbox for the SoC or something.

Instead, they can simply create an incident in Sentinel directly, which I think is a super cool way

of letting your users interact directly with the SoC.

I mentioned it already.

But there are a lot of pre-built connectors that you can use in ServiceNow, and those connectors really

have certain predefined actions that come with them that allow you to interact, for example, with

third party services or other Microsoft services.

So, for example, as we saw earlier, in the case of ServiceNow, when we select the ServiceNow connector,

it gives us the ability to create a new ticket in ServiceNow based on a predefined input, such as a

sentinel incident.

And the same is true for other connectors that you see here.

We saw also the example with Microsoft Forms, but you can really leverage those hundreds and hundreds

of connectors that already exist.

And what you currently see on the screen is just a a selection of connectors.

That is pretty common.

And it also includes services from other hyperscalers such as AWS.

In this case, we have an S3 connector, but there are also things like VirusTotal, which are pretty

common when talking about security automation.

So that's it.

Let's have a look at the demos.

![[Pasted image 20260731193743.png]]