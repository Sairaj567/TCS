Welcome back.

What we will do now is we will create our own Sentinel notebook.

And to do that we head over to notebooks.

And we select templates and then we select the Getting Started guide for Microsoft Central Machine Learning

notebooks.

We select that one and click on create from template.

And now you will see that we actually require an Azure Machine Learning workspace to deploy workbooks

in Sentinel.

So we will do exactly that.

We will now create a new Azure Machine Learning workspace.

Keep in mind that this will occur or additional costs depending on if you utilize the free credits or

not.

Just want to make you aware of this, this will cost additional money.

So we click on create a new ML workspace and we see that we are being forwarded to another dashboard

to create this one.

For that we select the same subscription same resource group and we name it Sentinel.

ML.

Workspace.

You will also see that.

There will be additional resources created automatically, such as a storage account, a key vault,

application insights, and we will actually keep this values.

If you already have a storage account or key vault, for example, that you want to utilize, feel free

to do that.

Just keep in mind, um, those, if you do not specify others will be created automatically for you.

If you create the machine learning workspace and then we head over to networking.

And we keep it with the public one in an enterprise environment.

It from a security perspective is better to go with private endpoints.

But this is just for testing, so we can easily leave it to public for the encryption.

We leave it with the Microsoft managed keys.

You could also decide to go with customer managed keys, but especially for demos like this, it is

a complete overkill in terms of complexity.

So we simply leave it to the Microsoft managed keys, and then we select next for the identity.

And here we can decide to go with a system assigned or a user assigned managed identity.

And in this case, as with the logic apps, we go with a system assigned managed identity for the storage

account access type.

We leave it in terms of simplification purposes to the credential based access, meaning access will

be conducted via a storage account access key.

In terms of security, it would be better, just as a disclaimer, to go with identity based access.

But again, for simplification purposes, we don't need to do it.

And we can also decide whether this is a high business impact workspace.

Of course, in our environment this is not the case.

Then we select next.

Tagging is something we leave empty and then we go on.

Next review and create and we wait for the validation to pass.

The validation has been passed.

So we click on create.

And we see that the deployment is currently initializing.

And we see that it's in progress, so let's wait for it to be finished.

And now we see that the deployment is complete.

Great.

Let's head over to Sentinel again.

And again into the notebook section.

And again on templates.

And then you select again the Getting Started guide.

And we click on create from template.

And now you will see that the workspace that we just created for Azure Machine Learning is now appearing

automatically.

We click on save.

And now the notebook will be deployed to your machine learning workspace.

It has been cloned successfully.

That is great.

And now we click on Launch Notebook.

And we see that we are being redirected again to the Azure AI Machine Learning workspace.

You will get a few prompts and then there are some things that we need to do.

Additionally, what we need to do first is we will need to create a new compute.

Because the code, the Python code that we execute in the machine learning workspace will be run on

compute instances.

And those are not existing by default.

But instead we need to create some.

Um, we could go with the serverless spark compute, which is something we won't do.

Instead we will create a new.

Option a new compute, and we go with the standard one if you want to.

You can also if it's like your private tenant can go with the cheaper one.

In this case I will go with the preferred solution.

And the compute name in my case will be Sentinel 100.

And then we click on Review and create and again on create.

And then you will see that under the compute section the compute is currently being created.

We need to wait for this one to be finished.

And after the creation has been successful, you will also see that the computer is running and that

also in the kernel section we actually have a Python kernel existing, which is important because otherwise

the code will not be executed.

Good.

So by now we have everything up and running.

To start with, the actual content of the notebook that you see on your screen.

And what we have here is a getting started notebook to introduce you to the idea of what are notebooks

and how can they be leveraged in Sentinel.

This notebook is actually walking you through various steps.

The first one is like explaining what is a Jupyter notebook and how can you actually run code?

And then afterwards we will initialize the notebook and a very, very nice library called Mystic Pi,

which is an open source Python library that is heavily also influenced by Microsoft.

And this Python library basically allows you to leverage threat intelligence as part of anything you

want to do that can be in Sentinel, but this can also be actually outside of Sentinel.

Um, this is a library that you can simply utilize as part of Python.

What we will do is we will initialize this one and the notebook, and then we will use it to query data

from Sentinel.

And in the last step we will also enrich it with data we get from VirusTotal and from geo light.

So meaning we will enrich the information that we have already with those two providers.

Great.

Let's start to do that.

And for that we simply scroll down a bit and you will see the first page being introduction.

No worries, no reason to go through it in detail.

If you want to, you can do it.

But I explained the basics of what our notebooks already to you so we can scroll down a bit and then

you see the first section where we can actually execute code.

And what you can see is that there is this window that actually contains Python code in this case we

do a simple calculation.

But what you can do actually in this browser window is you can execute this code.

So let's say you want to do this.

You already see that the output is already existing.

But we can simply select the window and then click on clear output.

Then the output will disappear.

And then we can click on the play icon to run the code again.

And then you will see great.

It has been executed.

And we also see the output from the print statement.

And we also see the number.

You can also edit this code in line.

So if I say I don't want to calculate two plus two, but instead I want to calculate two plus four,

I can simply edit this number, run it again.

And then we see that the output has changed.

Of course this calculation doesn't make sense because in the print statement we simply say still two

plus two equals six, which is of course wrong.

So we change that one back and execute it again.

And if we scroll down we can actually see that you can chain the output of those code windows together.

Because what we just did in the first window is we calculated two plus two and assigned the output to

our variable in this case y.

And then in the next code section we said please take the y and add two to it and give us an output.

And in this case this is six.

Which is great because it shows you that we can actually chain outputs across various windows.

So let's go ahead and scroll down a bit.

And then you see that we have to initialize this notebook and mystic pie in order to use it afterwards.

So to do that, we again scroll down a bit.

And what we will do is we will execute the first window.

What does it actually do?

Um, it will conduct some upgrades.

We don't need to go into detail into this.

And it will also initialize Mystic Pie, which is of course important to use it.

Later on there will be some configuration warnings, such as this one missing Mystic Pie Config.yaml.

Just ignore that this is completely expected.

Just execute the code.

And wait for it to finish.

Now that has been finished.

You see that you get a lot of error messages, which again, which is completely expected.

Not bad at all.

Just ignore this and keep scrolling down to the verification for the Microsoft Sentinel settings.

What we will do in this case we will verify the configuration of our Sentinel workspace.

And currently you see the output of a Sentinel workspace that was existing as part of an example for

this workbook.

But after we execute the statement what you should see is your current Sentinel workspace.

So what we do now is we click on.

The code execution again and wait for it to finish.

And then you will see, in my case, um, the various metadata that is associated with my sentinel tenant.

So for example, my resource group, my subscription ID, tenant ID, yada yada.

And this should not reflect what you currently see, but instead reflect your Sentinel workspace.

Great.

This has worked.

And now we scroll down again.

And what we can do here is configure Azure Cloud.

This is something we don't need to do because this is something you only need to do if you operate in

a sovereign or a government cloud, which in 99.9% of cases you won't because we will simply utilize

the public cloud offering from Azure.

And then we scroll down again and we see that we need to provide a query provider.

Um, but in this case we can simply execute the code.

And it has been finished.

And now we need to authenticate to the Sentinel workspace.

And for this one we can again.

Execute the code and you may get an error such as this one.

If you get this error, what you need to do is you need to go up and click on authenticate on this yellow

warning.

And then you will be redirected to another page.

And then you see, great.

You are now successfully authenticated in the green bar on the top.

And then we can execute the code again.

And then as you can see, this is actually working.

What does this statement do?

This statement, um, authenticates the notebook to your Sentinel workspace.

And this is why it's so important that a bit on the top we.

Executed the code to refresh the configuration and to initiate the connection to the central workspace.

Because we have authenticated successfully, we can now actually go ahead and start to test our connection

using the Mystic Pi built in Sentinel query.

To do that, we simply again click on the code execution.

And we see that it worked.

But the query returned no roles for this time range, which is absolutely no issue, but it's in fact

actually quite good because we see that the connection is working, and we can now utilize Mystic Pie

to query data from Sentinel, which is exactly what we will do in the next steps when we will configure

and test our external resource provider providers, which will be VirusTotal and G'u Light.

And now another optional step that you can do is for VirusTotal and Zeolite.

We will leverage API keys that allow us to authenticate to those services.

If you want to, you can store the API key in your Azure Key Vault that we created as part of the creation

step for the Azure Machine Learning workspace.

That is something I won't do because it just makes stuff more complex.

But if you want to, the instructions are pretty easy.

But for simplification purposes, we leave this out in this step and we provide instead the API keys

directly in the configuration.

Of course, for production use cases and something that you do in your enterprise, don't do it.

This is just for testing purposes and for demo purposes.

But this is something that you shouldn't do in production.

So if you scroll down and what we will do is we will configure our VirusTotal connection.

And to do that, just head over to VirusTotal.

The link is something you will find in the resources section of this video.

And then simply start to create your account.

And then we will have a look at the API key.

And then after you have created your account and logged in, you can view your API key.

If you look at your profile and then click on API key.

And then just take the API key and copy it.

After you have created your account, we simply head back to the machine learning workspace and we now

again execute those two statements to initialize the configuration.

And now we will add VirusTotal as a threat Intel provider.

And that can be done by executing this code and then clicking on this box.

Selecting VirusTotal from the dropdown menu.

Clicking on add.

And then you will see that we are prompted for the value of our API key.

If you just head over to VirusTotal, select your profile icon, click on API key.

You will see that the key is being displayed.

You copy the key and you paste it in here.

And after that you click on update.

And then again on save settings.

So now your API key should have been saved.

And now we can scroll down a bit and we will do the same.

Now with the Geoip provider, which is going to be Geoip lite in this case.

For this we head over to the page of Geoip Lite.

You will find the link for that in the resources section of this video.

Same with VirusTotal.

We head over to the website and then we click on sign up for Geo Lite two.

And we provide our details here.

And then after you created your account, what you need to do is you need to go on manage license keys

and create a new license key.

And this one is then used to authenticate against this service from our notebook.

So we click on Generate new license key.

And then we copy it again over to our notebook.

After you've copied your key what you will do is you will click on this box.

Click on add for the Geoip light and then you will copy your API key over and again click on update.

And save settings.

And then we are done with the configuration of our threat Intel provider, which is VirusTotal, and

our geo provider, which is in this case Geo Lite.

And then we scroll down and test this one.

To that we simply execute the code for the VirusTotal lookup.

And then you get an error that at this point is expected.

But this one can easily be fixed by simply putting in mystic pi and a dot in front of the t lookup.

And then you click on Run Cell again, and then you see that it actually works.

So what did we do?

We took an IP address, this one, and asked VirusTotal to give us the results for this IP address.

And if we scroll down, you will see the results that we get from VirusTotal.

To do that, and we can also have a look at the raw results.

And then we see a lot more info with regards to this IP.

This one is of course hardcoded.

You can also change it.

Let's take one that is obviously not malicious, which is for example one of the Google DNS servers.

Let's execute the code again.

And then we see that a lookup is being conducted for VirusTotal.

And we also see the raw data.

And then we do the same for the IP lookup.

For that we scroll down a bit.

And if we execute the code, you will see the exact same error.

Before that, the name is not defined basically.

So to fix that we simply again type mystic pi and the dot and we execute the code again.

And then we see the geo lookup with regards to the IP that was provided for the notebooks earlier.

In this case this IP is originating from Berlin, Germany.

And we see some additional information with regards to the geo location.

That was it.

That was the first introduction with regards to how to leverage notebooks in Sentinel.

As a quick recap, what did we actually do?

We leveraged Mystic Pi, which is a threat intelligence library, to query data from our Sentinel notebooks,

um, against VirusTotal and Geo Lite, which is a great thing to do.

And I think it shows the power of notebooks in quite a good way.