All right.

So let's talk about the next layer in the middle attack framework which is the techniques.

And this is really the how the adversary performs the attack.

Before we talked about the tactics.

That is the why.

Now we talk about the techniques.

That is the how.

And in contrast to the tactics where we only had 14 with the techniques, we currently have 201 techniques.

Of course, this is subject to change over time.

There will be more techniques being added.

But the time of this recording, we have 201 techniques and we will have a look at a few of those techniques.

This is just examples.

Of course I don't will walk you through all 201 techniques.

Instead we will have a look at a few selected ones that are quite typical and quite representative for

the various tactic.

So what you see here is of course a match, because every technique also belongs to a tactic.

And if we look from top to bottom, starting with reconnaissance, a very typical technique for detecting

reconnaissance is the active scanning.

Um, this is like vulnerability scanning or other things just scanning to gather more information for

the tactic of resource development.

A typical technique is of course, to develop capabilities.

Could be exploits, could be infrastructure.

And so on.

Then for the tactics, we have the initial access that could be fishing for the execution.

We have things like a scheduled task in Active Directory.

This one will, for example, allow you to distribute software.

Then we have persistence.

And this one a common common technique is for example, creating accounts for privilege escalation.

We have things like escape to host.

So for example from a virtual machine escaping to the host system.

Then for the defense evasion we have things like masquerading.

This could be for example by adding a double extension.

This could be by encrypting.

There could be several things.

For credential access we have things like brute force as a technique.

So simply a brute force attack on the accounts to get their credentials.

For discovery we have things like account discovery.

This could be for example, you enumerating accounts via a PowerShell command.

Then for the lateral movement we have things like internal spearfishing.

This could be an attacker sending internal spearfishing mails to high priority targets, which is of

course a lot harder for the victim to identify as a fish.

Then we have for the collection things like email collection, where you simply collect a lot of mails

and try to identify, um, important intelligence for you via mails.

Then for common command and control we have very classical example which would be an encrypted channel.

Um, so that.

Traffic cannot be inspected by the defenders.

Then we have things like X for the exfiltration.

We have things like exfiltration over your command and control channel.

C2 is the abbreviation for command and control here in this case and for the impact we have things like

data destruction for example.

Again this is just a few examples.

We will have a look at selected techniques in more detail later on during our demos and the case study. 

![[Pasted image 20260726191040.png]]