Good.

Let's talk about the sub techniques in more detail.

And for the sub techniques this is again as with the techniques the exact how an adversary performs

its attack.

But it's way more detailed than the techniques.

And you will see some examples in a few seconds.

In total, there are 424 sub techniques at the time of this recording.

Same logic as with tactics and techniques.

This is subject to change.

There will be more added over time.

Let's have a look at a few selected sub techniques.

And as you see, I took most of the techniques that we talk about earlier.

Um, and match them to the sub techniques.

Some have changed, but most of them stayed the same.

So for example, for the tactic reconnaissance and the technique active scanning, there is a sub technique

existing called vulnerability scanning.

And this one we would of course scan systems for vulnerabilities for the tactic resource development

and the technique develop capabilities.

We would of course have things like malware where we set up our malware for the initial access and phishing

combination.

We would have something like a spear phishing attachment.

So this would be an attacker sending a crafted phishing email to a high priority target that contains

malware as part of an attachment for the execution tactic and the schedule task technique, you could

have things like a cron job.

This could, for example, be a cron job on a Linux system that, um, does a few bad things.

Then we follow the tactic, persistence and the technique.

Create accounts have things like a local account.

This could be, for example, you creating a local administrator account or simply a local um non-privileged

account on a system for detecting privilege escalation and the technique process injection.

We have very common example which is dynamic link library injection.

So DLL injection this would be an attacker injecting a DLL into a system.

And the DLL would do of course malicious stuff for the tactic defense evasion and the technique masquerading.

We have things like a double file extension.

This could be for example you masquerading an executable.

Um, that is an executable, but this would be you adding another file extension, for example PDF to

simply masquerade it.

Then for the tactic credential access and the technique brute force.

We have things like password spraying.

So this would be you leveraging, um, a brute force attack to spray passwords across the organization

that you try to compromise for the tactic discovery and the technique account discovery.

We have things like cloud accounts.

This would be you for example enumerating accounts and enter ID.

And then for lateral movement and remote services.

We have another very classical example which would be the Remote Desktop Protocol.

This would be you, um, from an already compromised host trying to see which servers you can access

via RDP, for example.

Then we have the collection tactic and the email collection technique.

And the sub technique for this one would be, for example, you, um, gathering remote emails for command

and control and the encrypted channel.

We have a sub technique called asymmetric cryptography.

This would just be you leveraging asymmetric cryptography such as, uh, RSA or for example, Elgamal

or elliptic curves, um, to encrypt your channel with, for exfiltration and exfiltration.

Over the alternative protocol we have things like exfiltration of an asymmetric encrypted non C2 protocol.

So again leveraging asymmetric cryptography and a non C2 protocol to exfiltrate your data.

And then for impact we have uh another classical example which would be network denial of service.

And as a sub technique this would be to flood the network directly.

Again this is just a few examples of sub techniques.

And as with the tactic and the techniques, we have a way more closer look on selected sub techniques

later on during the demo and the case study.