In contrast to data sources, we also have the detections in mid-air, and the main purpose of detections

is to give you a high level detection strategy for the TPS that the adversaries are leveraging.

It's also especially focused on techniques and sub techniques because, again, techniques and sub techniques

are the how an adversary achieves its objective.

And of course the how part can be detected.

And it also should give you a guideline on what to actually do with the collected telemetry.

Um, and in this case, if we talk about the tactic technique and the sub technique again that we leveraged

before, which is reconnaissance, active scanning and vulnerability scanning, in terms of detecting

stuff, we would have to look at the network traffic in more detail on the actual content and the flow.

Um, I brought up the example of the web application firewall logs earlier.

That is exactly what you would leverage as a data source.

But this is also what you would leverage in terms of detection.

So if you would look, um, for detecting vulnerability scans, you would have to write a rule in your

CIM system that tells, hey, whenever we detect a scan based on the logs that we ingest from the web

application firewall, then just throw us an alert.

And that is how you can approach the Https that the attackers leverage with data sources and detections.

![[Pasted image 20260726191818.png]]